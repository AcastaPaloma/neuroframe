import '@fontsource/barlow/latin-400.css';
import '@fontsource/barlow/latin-500.css';
import '@fontsource/barlow/latin-600.css';
import '@fontsource/barlow-condensed/latin-600.css';
import './style.css';
import './embodied.css';
import './live.css';
import type {LiveProfile} from './live-profile';
import {BrainView} from './brain';
import {FlyBodyView} from './body';
import {LiveSource} from './live-source';
import {fetchJson,type BrainMetadata} from './types';

const base=import.meta.env.BASE_URL,app=document.getElementById('live-experiment')!;
const $=<T extends HTMLElement>(id:string)=>document.getElementById(id) as T;
app.innerHTML=`
<a class="skip-link" href="#live-main">Skip to the experiment</a>
<header class="app-bar"><a class="brand" href="${base}"><img src="${base}favicon.svg" alt="" width="32" height="32"/><span>NEUROFRAME</span></a><nav class="experiment-nav" aria-label="Experiments"><a href="${base}embodied.html">Whole-branch baseline</a><a href="${base}sites.html" aria-current="page">Selected-site experiment</a></nav></header>
<main id="live-main">
 <div class="workspace-heading"><div><h1>Earlier selected-site experiment.</h1><p>A live stimulation controller, simulated synaptic spikes, and a physical fly model.</p></div><a class="method-link" href="#live-method">How to read this</a></div>
 <div class="experiment-grid">
  <section class="experiment-viewers" aria-label="Live neural display and body">
   <div class="paired-stage">
    <section class="neural-stage"><div class="stage-title"><h2>The brain</h2><select id="live-brain-view" aria-label="Brain viewpoint" disabled><option value="image">Image close-up</option><option value="full">Full brain</option><option value="oblique">Angled view</option><option value="side">Side view</option></select></div><div id="live-brain" class="brain-canvas"></div><div class="stage-caption"><strong>Light from simulated spikes</strong><span id="site-caption">Loading real neuron observation sites</span></div></section>
    <section class="body-stage"><div class="stage-title"><h2>The body</h2><select id="live-body-view" aria-label="Body viewpoint" disabled><option value="angled">Angled view</option><option value="front">Front</option><option value="side">Side</option><option value="top">Top</option></select></div><div id="live-body" class="brain-canvas"></div><div class="stage-caption">NeuroMechFly / MuJoCo<span>Descending neurons → gait controller → physics</span></div></section>
    <div id="live-loading" class="loading-state" role="status"><strong id="live-loading-title">Preparing the live model</strong><span id="live-loading-detail">Loading measured connectivity and anatomy…</span><button id="live-retry" class="button" hidden>Reload</button></div>
   </div>
   <div class="experiment-stats"><span><strong id="live-fps">—</strong> new neural frames/s</span><span><strong id="live-latency">—</strong> ms processing latency</span><span><strong id="live-speed">—</strong>× model time</span><span><strong id="live-resolution">—</strong> target pixels</span></div>
   <div class="experiment-transport"><button id="live-play" class="button primary" disabled>Start experiment</button><button id="live-reset" class="button" disabled>Reset</button><div class="experiment-timeline"><label for="live-seek">Source time <strong id="live-source-time">0.00 s</strong></label><input id="live-seek" type="range" min="0" max="9.5" step="0.01" value="0" disabled/></div><span id="live-model-time">0.000 s simulated</span></div>
   <p class="experiment-clock-note" id="live-clock">Video follows the wall clock. The controller processes the newest available frame; no queue of old video frames builds up. Measured throughput includes the connected body.</p>
   <section class="readout" aria-label="Live model output"><div class="readout-heading"><h2>Signals leaving the brain</h2><span id="live-events">Waiting for spikes</span></div><div class="readout-grid">${[['left','DNa02 · left'],['right','DNa02 · right'],['forward','DNp09 · forward'],['back','MDN · backward']].map(([id,label])=>`<div><span>${label}</span><strong id="live-dn-${id}">0.0 Hz</strong><meter id="live-meter-${id}" min="0" max="300" value="0" aria-label="${label} firing rate"></meter></div>`).join('')}</div></section>
  </section>
  <aside class="inspector experiment-inspector" aria-label="Clip and synapse controls">
   <section class="source-section"><div class="section-heading"><h2>Your stimulus</h2><span class="small-label">Local video</span></div><div class="source-monitor"><canvas id="live-stimulus" width="320" height="240" role="img" aria-label="Video source supplied to the stimulation controller"></canvas><span id="live-source-badge">FREEDOOM</span></div><p class="control-help" id="live-reference-time">Reference supplied to the controller</p><label for="live-clip" class="field-label">Example clip</label><select id="live-clip" class="clip-select" disabled></select><label for="live-file" class="field-label">Or choose your own DOOM clip</label><input id="live-file" type="file" accept="video/*" disabled/><p id="live-file-status" class="control-help" role="status">Decoded on this computer. Your file is not uploaded.</p><div class="source-facts"><span id="live-source-size">—</span><span id="live-source-duration">—</span></div></section>
   <section class="display-section"><h2>What lights up</h2><label for="live-representation" class="field-label">Anatomical display</label><select id="live-representation" class="clip-select" disabled><option value="sites">Fixed observation sites</option><option value="branches">All sampled branches</option></select><p class="control-help">Each marker sits on a real neuron and uses that neuron's own spike history. Sites were selected once for coverage, independently of the video. The branch view reveals the same activity across each whole arbor.</p><label class="check-row"><input id="live-soften" type="checkbox" checked disabled/><span>Soften point speckle</span></label><p class="control-help">A small, fixed blur of the emitted light. Turn it off to inspect individual sites.</p><p id="live-model-summary" class="control-help">Loading the full connectome…</p><p id="live-selection" class="control-help"></p></section>
   <section class="causal-controls"><h2>Test the connection</h2><label class="check-row"><input id="live-synapses" type="checkbox" checked disabled/><span>Synaptic transmission</span></label><label class="check-row"><input id="live-stimulation" type="checkbox" checked disabled/><span>Video stimulation</span></label><p class="control-help">Disable transmission and reset: the displayed neurons should stay dark even while video stimulation continues. Changing the source while paused preserves the brain's state.</p><button id="live-save" class="button" disabled>Save brain view</button></section>
  </aside>
 </div>
 <section id="live-method" class="science-method"><div><h2>Every light has a neuron behind it.</h2><p>The source is converted to a grayscale target. The external controller chooses pulse trains on a separate set of real upstream cells. Ideal voltage clamps enforce those pulses and cancel incoming currents on the actuated cells. Displayed neurons and motor outputs are excluded from these clamps. A leaky integrate-and-fire simulation propagates their spikes through the published FlyWire connections. The renderer receives only the resulting spike traces and anatomical coordinates.</p><p>The sharp view samples one fixed site on each neuron. It does not show every branch glowing. You can switch to the full sampled arbors, where long branches spread the same activity over a wider area. Neither view changes the anatomical coordinates. The reference projection stays fixed while you orbit.</p><p>This is idealized external voltage control of a simplified computational model, not natural fly vision or a demonstrated procedure for a living fly. Displayed cells and descending motor outputs receive no direct video stimulation. The camera, upstream actuators and observation-site placement are engineered parts of the experiment.</p><div class="science-links"><a href="${base}data/live-validation/comparison.png" target="_blank">Source vs. measured spikes</a><a href="${base}data/live-validation/sequence-summary.json" target="_blank">Sequence measurements</a><a id="live-provenance" href="${base}data/live-profile.json" target="_blank">Anatomy and coverage</a><a href="https://github.com/philshiu/Drosophila_brain_model" target="_blank" rel="noreferrer">Reference brain model</a><a href="https://neuromechfly.org/" target="_blank" rel="noreferrer">NeuroMechFly</a></div></div><div><h2>Measure the live result.</h2><p>New neural frames/s counts newly simulated outputs associated with distinct decoded video frames. Processing latency runs from source capture to submitting its measured neural image for display. It excludes video decoding and the spike trace's temporal smoothing. These numbers are separate from the screen refresh rate.</p><p>The body integrates physical joints and contacts in a separate worker. It follows the same sequence of simulated brain outputs, one processing stage behind. The descending-neuron-to-gait adapter is engineered; the ventral nerve cord, natural muscles and body-to-brain feedback are not reconstructed.</p><p>Playback preserves neural history across cuts and clips. Reset clears both simulations. Brightness uses an exponential spike trace, so abrupt changes can smear. High source resolution does not guarantee that the anatomy and controller can reproduce all its detail.</p><p class="license-note">FlyWire: CC BY 4.0. Shiu code: MIT. NeuroMechFly / MuJoCo: Apache 2.0. Included game clips: Freedoom Phase 2, BSD-3-Clause; <a href="${base}data/live-clips/COPYING">copyright notice</a>. The body and brain came from different fly specimens.</p></div></section>
</main>`;

let profile:LiveProfile;let pendingReference:ImageData|null=null;
let brain:BrainView,body:FlyBodyView,source:LiveSource,neural:Worker,physics:Worker,anatomy:BrainMetadata;
let ready=false,playing=false,neuralBusy=false,bodyBusy=false,waitingBody:any=null,epoch=0,changing=false;
let timer=0,raf=0,nextCapture=0,lastSerial=-1,frameId=0,neuralTime=0,completed=0,lastCompleted=0,sampleTime=performance.now(),lastModelTime=0;
let latencies:number[]=[],computeTimes:number[]=[],bodyTimes:number[]=[],measuredFrames=0;
const samples:{at:number;source:number;latency:number;decodedLatency:number;compute:number;body:number;frame:number}[]=[];
function setPlaying(value:boolean){playing=value;$('live-play').textContent=value?'Pause experiment':'Start experiment';app.dataset.playing=String(value);if(value){nextCapture=performance.now();void source.video.play().catch(error=>{setPlaying(false);$('live-file-status').textContent=String(error);});}else source?.video.pause();}
function config(){neural?.postMessage({type:'config',synapses:$<HTMLInputElement>('live-synapses').checked,muted:!$<HTMLInputElement>('live-stimulation').checked});}
function fail(error:unknown){ready=false;neuralBusy=false;bodyBusy=false;waitingBody=null;setPlaying(false);$('live-loading').hidden=false;$('live-loading-title').textContent='The live experiment stopped';$('live-loading-detail').textContent=error instanceof Error?error.message:String(error);$('live-retry').hidden=false;app.dataset.ready='error';}
function startBody(){if(!waitingBody||bodyBusy)return;const m=waitingBody;waitingBody=null;bodyBusy=true;physics.postMessage({type:'step',epoch,ms:100,left:m.motor.left,right:m.motor.right});}
async function idle(){while(neuralBusy||bodyBusy||waitingBody)await new Promise(r=>setTimeout(r,20));}
async function reset(){setPlaying(false);changing=true;await idle();epoch++;neuralTime=0;completed=0;lastCompleted=0;measuredFrames=0;lastModelTime=0;sampleTime=performance.now();samples.length=0;latencies=[];pendingReference=null;app.dataset.frames='0';app.dataset.maximumActivity='0';app.dataset.spikes='0';app.dataset.deliveries='0';app.dataset.measurements='[]';$('live-fps').textContent='—';$('live-latency').textContent='—';$('live-speed').textContent='—';$('live-events').textContent='Waiting for spikes';neural.postMessage({type:'reset',epoch});physics.postMessage({type:'reset',epoch});source.video.currentTime=0;brain.setNeuralActivity(new Float32Array(anatomy.neuronCount));app.dataset.simTime='0';lastSerial=-1;$('live-model-time').textContent='0.000 s simulated';changing=false;}
async function selectVideo(video:string|File,name:string){const resume=playing;setPlaying(false);changing=true;try{if(!await source.load(video))return;if(typeof video==='string')$<HTMLInputElement>('live-file').value='';$('live-reference-time').textContent=`Source preview · ${source.video.currentTime.toFixed(2)} s · awaiting simulation`;$('live-file-status').textContent=name;$('live-source-badge').textContent=video instanceof File?'YOUR CLIP':'FREEDOOM';$('live-source-size').textContent=`${source.video.videoWidth} × ${source.video.videoHeight} source`;$('live-source-duration').textContent=`${source.video.duration.toFixed(1)} s`;$<HTMLInputElement>('live-seek').max=String(source.video.duration-.01);lastSerial=-1;if(resume)setPlaying(true);}catch(error){$('live-file-status').textContent=error instanceof Error?error.message:String(error);}finally{changing=false;}}
$('live-play').onclick=()=>setPlaying(!playing);$('live-reset').onclick=()=>void reset().catch(fail);$('live-retry').onclick=()=>location.reload();
$('live-soften').onchange=()=>brain.setActivityBlur($<HTMLInputElement>('live-soften').checked?.6:0);
$('live-synapses').onchange=config;$('live-stimulation').onchange=config;
$('live-brain-view').onchange=()=>brain.goTo($<HTMLSelectElement>('live-brain-view').value as 'image');
$('live-body-view').onchange=()=>body.setView($<HTMLSelectElement>('live-body-view').value);
$('live-file').onchange=()=>{const file=$<HTMLInputElement>('live-file').files?.[0];if(file)void selectVideo(file,file.name);};
$('live-seek').oninput=()=>{source.video.currentTime=Number($<HTMLInputElement>('live-seek').value);};
$('live-save').onclick=()=>void brain.saveImage().then(blob=>{const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='neuroframe-spikes.png';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);});
$('live-representation').onchange=async()=>{const select=$<HTMLSelectElement>('live-representation');select.disabled=true;try{const sites=select.value==='sites';await brain.changePointGeometry(`${base}data/${profile.anatomyDirectory}${sites?profile.observationPoints:'brain-points.bin'}`,sites?anatomy.neuronCount:anatomy.pointCount);$('site-caption').textContent=`${anatomy.neuronCount.toLocaleString()} neurons · ${sites?'one fixed site each':'whole sampled arbors'}`;}catch(error){fail(error);}finally{select.disabled=false;}};
async function start(){
 try{
  profile=await fetchJson<LiveProfile>(`${base}data/live-profile.json`);
  const [meta,view,clips]=await Promise.all([fetchJson<BrainMetadata>(`${base}data/${profile.anatomyDirectory}brain.json`),fetchJson<{normal:number[];cellMicrometers:number;resolution:number[]}>(`${base}data/${profile.view}`),fetchJson<{clips:{id:string;name:string;file:string}[]}>(`${base}data/live-clips/clips.json`)]);anatomy=meta;
  $('live-resolution').textContent=view.resolution.join(' × ');app.dataset.profile=profile.id;$('live-selection').textContent=profile.disclosure;$<HTMLAnchorElement>('live-provenance').href=`${base}data/${profile.view}`;
  source=new LiveSource($('live-stimulus'),view.resolution[0],view.resolution[1]);source.video.addEventListener('seeked',()=>{source.capture(!playing);if(!playing)$('live-reference-time').textContent=`Source preview · ${source.video.currentTime.toFixed(2)} s · paused`;});
  for(const clip of clips.clips){const o=document.createElement('option');o.value=clip.file;o.textContent=clip.name;$('live-clip').append(o);}
  $('live-clip').onchange=()=>{const select=$<HTMLSelectElement>('live-clip');void selectVideo(`${base}data/${select.value}`,select.selectedOptions[0].text);};
  brain=new BrainView($('live-brain'),$('live-stimulus'),{...meta,pointCount:meta.neuronCount,lineVertexCount:0});brain.setNeuralActivity(new Float32Array(meta.neuronCount));brain.setActivityNormalization(true,view.cellMicrometers);brain.setImageView(view.normal);brain.setActivityBlur(.6);
  brain.onContextLost=()=>fail(Error('Graphics context lost. Reload the experiment.'));body=new FlyBodyView($('live-body'));
  neural=new Worker(new URL('./live-neural.worker.ts',import.meta.url),{type:'module'});physics=new Worker(new URL('./body.worker.ts',import.meta.url),{type:'module'});
  let neuralReady:()=>void,bodyReady:()=>void;let rejectNeural:(e:Error)=>void,rejectBody:(e:Error)=>void;
  const nready=new Promise<void>((resolve,reject)=>{neuralReady=resolve;rejectNeural=reject;}),bready=new Promise<void>((resolve,reject)=>{bodyReady=resolve;rejectBody=reject;});
  neural.onmessage=event=>{const m=event.data;if(m.type==='error'){neuralBusy=false;const error=Error(m.message);rejectNeural(error);fail(error);return;}if(m.type==='ready'){$('live-model-summary').textContent=`${m.neurons.toLocaleString()} neurons simulated · ${m.edges.toLocaleString()} measured connections · ${m.inputs.toLocaleString()} upstream spike actuators.`;app.dataset.backend=m.adapter;neuralReady();return;}if(m.type!=='state')return;neuralBusy=false;if(m.epoch!==epoch)return;
   neuralTime=m.timeMs;brain.setNeuralActivity(m.values);brain.render(performance.now(),0);waitingBody=m;startBody();const latency=performance.now()-m.capturedAt;latencies.push(latency);computeTimes.push(m.computeMs);
   completed++;measuredFrames++;if(pendingReference){source.present(pendingReference);pendingReference=null;}$('live-reference-time').textContent=`Reference for this neural frame · ${m.sourceTime.toFixed(2)} s`;
   samples.push({at:performance.now(),source:m.sourceTime,latency,decodedLatency:performance.now()-m.decodedAt,compute:m.computeMs,body:bodyTimes.at(-1)??0,frame:m.frameId});if(samples.length>600)samples.shift();
   app.dataset.simTime=String(neuralTime);app.dataset.spikes=String(m.spikes);app.dataset.deliveries=String(m.deliveries);app.dataset.frames=String(measuredFrames);app.dataset.maximumActivity=String(Math.max(...m.values));
   app.dataset.measurements=JSON.stringify(samples.slice(-120));$('live-model-time').textContent=`${(neuralTime/1000).toFixed(3)} s simulated`;$('live-events').textContent=`${m.spikes.toLocaleString()} spikes · ${m.deliveries.toLocaleString()} deliveries`;
   const mean=(type:string,side?:string)=>{const rows=m.motor.rates.filter((n:{cellType:string;side:string})=>n.cellType===type&&(!side||n.side===side));return rows.reduce((s:number,n:{hz:number})=>s+n.hz,0)/Math.max(1,rows.length);};
   for(const [id,value] of [['left',mean('DNa02','left')],['right',mean('DNa02','right')],['forward',mean('DNp09')],['back',mean('MDN')]] as const){$(`live-dn-${id}`).textContent=`${value.toFixed(1)} Hz`;$<HTMLMeterElement>(`live-meter-${id}`).value=value;}
  };
  physics.onmessage=event=>{const m=event.data;if(m.type==='error'){bodyBusy=false;const error=Error(m.message);rejectBody(error);fail(error);return;}if(m.type==='ready'){bodyReady();return;}bodyBusy=false;if(m.epoch!==epoch)return;body.applyPhysicalState(m);bodyTimes.push(m.computeMs);app.dataset.bodyState=JSON.stringify(body.getState());startBody();};
  neural.onerror=e=>{rejectNeural(Error(e.message));fail(e.message);};physics.onerror=e=>{rejectBody(Error(e.message));fail(e.message);};
  neural.postMessage({type:'init',base:`${base}data/`,profile});physics.postMessage({type:'init',runtime:new URL(`${base}body/runtime.js`,location.href).href,assets:`${base}body/assets`,timestep:profile.bodyTimestep});
  await Promise.all([brain.load(()=>{},`${base}data/${profile.anatomyDirectory}`,profile.observationPoints),body.load(text=>{$('live-loading-detail').textContent=text;}),selectVideo(`${base}data/${clips.clips[0].file}`,clips.clips[0].name),nready,bready]);
  ready=true;app.dataset.ready='true';$('live-loading').hidden=true;document.querySelectorAll<HTMLInputElement|HTMLSelectElement|HTMLButtonElement>('input:disabled,select:disabled,button:disabled').forEach(n=>n.disabled=false);$('site-caption').textContent=`${meta.neuronCount.toLocaleString()} neurons · one fixed site each`;config();
  const loop=()=>{timer=window.setTimeout(loop,5);const now=performance.now();if(!ready||!playing||changing||document.hidden||neuralBusy||waitingBody||now<nextCapture||source.video.readyState<2)return;
   if(source.frameSerial===lastSerial)return;lastSerial=source.frameSerial;nextCapture=now+90;
   const capturedAt=performance.now(),values=source.capture(false);pendingReference=source.lastCapture;neuralBusy=true;neural.postMessage({type:'step',epoch,values,capturedAt,decodedAt:source.decodedAt,sourceTime:source.mediaTime,frameId:++frameId},[values.buffer]);
  };loop();
  let previous=performance.now();const animate=(now:number)=>{raf=requestAnimationFrame(animate);if(document.hidden||now-previous<1000/30)return;brain.render(now,Math.min(.1,(now-previous)/1000),true);body.render(true);previous=now;
   if(source.video.readyState>=2){$('live-source-time').textContent=`${source.video.currentTime.toFixed(2)} s`;$<HTMLInputElement>('live-seek').value=String(source.video.currentTime);}
   if(now-sampleTime>=1000){const elapsed=now-sampleTime;$('live-fps').textContent=((completed-lastCompleted)*1000/elapsed).toFixed(1);$('live-speed').textContent=((neuralTime-lastModelTime)/elapsed).toFixed(2);$('live-latency').textContent=latencies.length?String(Math.round(latencies.reduce((a,b)=>a+b)/latencies.length)):'—';sampleTime=now;lastCompleted=completed;lastModelTime=neuralTime;latencies=[];computeTimes=[];bodyTimes=bodyTimes.slice(-10);}
  };raf=requestAnimationFrame(animate);if(!new URL(location.href).searchParams.has('paused')&&!matchMedia('(prefers-reduced-motion: reduce)').matches)setPlaying(true);
 }catch(error){fail(error);}
}
document.addEventListener('visibilitychange',()=>{if(document.hidden&&playing)setPlaying(false);});
window.addEventListener('pagehide',event=>{if(event.persisted)return;clearTimeout(timer);cancelAnimationFrame(raf);neural?.terminate();physics?.terminate();source?.dispose();brain?.dispose();body?.dispose();});
void start();
